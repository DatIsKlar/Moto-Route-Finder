version: '3.8'
services:
  moto-route-finder:
    build: .
    container_name: moto-route-finder
    ports:
      - "5000:5000"
    volumes:
      - /home/user/MotorTour/maps:/data/maps
    environment:
      - ASPNETCORE_ENVIRONMENT=Production
      - MAPS_DIR=/data/maps
      - PORT=5000
    logging:
      driver: json-file
      options:
        max-size: "10m"
        max-file: "3"
    restart: unless-stopped
